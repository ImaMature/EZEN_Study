package Day09;

public class Day09_3 {
	//p.338 연습문제
		//5. 인수가 달라서 child.java의 5번째 줄에 this.name = super(name)으로 변경 : 자식클래스에서 부모클래스의 생성자 호출을 안 했기 때문
				// 만약에 부모클래스에 생성자가 없으면 컴파일 오류 안남
		//6. Da09_1 package -> Parent, Child, ChildExample 자바 파일 보기
		//7.
		//8. 1. B b = new B(); -> 가능 기본 생성자
			//2. B b = (B) new A(); -> 불가
				// * A a = new A(); -> 가능
				// * B b = (B)a; -> 가능
	
}
