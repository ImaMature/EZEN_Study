//p.386 확인문제
	/*1번 
	1) 인터페이스는 객체 사용 설명서 역할을 한다.
	2) 구현클래스가 인터페이스의 추상 메소드에 대한 실체 메소드를 가지고 있지 않으며 추상 클래스가 된다.
	답 3번 인터페이스는 인스턴스필드를 가질 수 없다. 상수와 메소드만 가질 수 있다.
	4) 구현 객체가 구현 된게 인스턴스
	 구현 객체 : rc = new TV();
	
	
	
	2. 인터페이스의 다형성
	 1) 필드가 인터페이스 타입일 경우 다양한 구현 객체를 대입할 수 있다.
	 2) 매개 변수가 인터페이스 타입일 경우 다양한 구현 객체를 대입할 수 있다.
	 3) 배열이 인터페이스 타입일 경우 다양한 구현 객체를 저장할 수 있다.
	 ex) RemortControl [] controls = {new TV(); new Audio();}
	 	 controls[0].turnOn(); -> 0번째인덱스(TV)의 turnOn()메소드 실행
	 4) 구현객체는 인터페이스 타입으로 자동 변환된다. 
	 
main
	*/